{
  "name": "@rsl-innovation/mem-port",
  "version": "1.0.2",
  "description": "A portable, long-term agentic memory MCP server — a thumb drive for your AI context.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rsl-innovation/mem-port.git"
  },
  "bugs": {
    "url": "https://github.com/rsl-innovation/mem-port/issues"
  },
  "homepage": "https://mem-port.com/",
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "mem-port": "bin/mem-port.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "build:ui": "node scripts/build-app-ui.mjs",
    "prebuild": "npm run build:ui",
    "build": "tsup",
    "prepublishOnly": "npm run build",
    "preversion": "npm run typecheck && npm test",
    "predev": "npm run build:ui",
    "dev": "tsx src/cli.ts serve",
    "pretest": "npm run build:ui",
    "test": "MEM_PORT_MODEL_CACHE_DIR=$PWD/.test-model-cache vitest run",
    "pretypecheck": "npm run build:ui",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.ui.json"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@surrealdb/node": "^3.0.3",
    "surrealdb": "^2.0.8",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.23.1",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "optionalDependencies": {
    "pg": "^8.23.0"
  }
}
